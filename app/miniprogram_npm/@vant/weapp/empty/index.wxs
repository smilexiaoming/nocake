/* eslint-disable */
var PRESETS = ['error', 'search', 'default', 'network'];

function pic_url(image) {
  if (PRESETS.indexOf(image) !== -1) {
    return 'https://img.yzcdn.cn/vant/empty-image-' + image + '.png';
  }

  return image;
}

module.exports = {
  pic_url: pic_url,
};

